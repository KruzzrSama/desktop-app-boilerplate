import React, { useEffect } from "react";
import { useNavigate } from "react-router";
import styles from "../assets/styles/app.module.css";

const App: React.FC = () => {
    return (
        <div className={styles.appLayout}>App</div>
    );
};

export default App;